# Toolchain

`bootstrap-tools.json` pins only the repository bootstrap. `sources.lock.json`
tracks candidate/frozen source closure. P003 owns the final host/target
toolchain selection and sealed mirror.
