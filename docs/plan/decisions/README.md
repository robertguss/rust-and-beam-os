# Gate decisions

Store one immutable Markdown decision per completed gate, named `gate-N.md`. Each decision records the outcome, evidence paths, accepted residual risks, approver, and authorizing commit.

This directory is empty until a human passes GATE-0.
